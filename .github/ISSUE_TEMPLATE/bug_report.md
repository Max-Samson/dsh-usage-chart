---
name: Bug report
about: Report something that is broken or misbehaving
title: ''
labels: bug
assignees: ''
---

**Environment**

- DSH version (run `dsh --version`):
- Node.js version (`node --version`):
- OS / browser:
- Install method: npm registry / GitHub / local directory
- Plugin version (if published):

**Describe the bug**

A clear and concise description of what is broken.

**To reproduce**

Steps to reproduce the behavior (e.g. open a session with token usage, click ▸, switch language/theme).

**Expected behavior**

What you expected to happen instead.

**Screenshots**

If applicable — but never include account balances, API keys, or private session data
(see [SECURITY.md](../../SECURITY.md)).

**Additional context**

Any plugin config (`cordis.patch.yml`), error output from the browser console or the
DSH log, etc.
